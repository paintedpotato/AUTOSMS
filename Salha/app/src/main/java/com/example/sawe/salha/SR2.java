package com.example.sawe.salha;
/*
import android.util.Config;
import android.app.PendingIntent;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.os.Bundle;
import android.telephony.SmsManager;
import android.telephony.SmsMessage;
import android.util.Log;
import android.widget.Toast;
import android.database.sqlite.SQLiteDatabase;

import android.content.IntentFilter;

//import android.database.Cursor;
import android.widget.TextView;

/**
 * Created by Sawe on 2/15/2016.
 *//*
public class SR2 extends BroadcastReceiver {

        /*
        Intent i = new Intent(getApplicationContext(), MainActivity.class);
        i.putExtra("msgContent", msg);
        i.putExtra("sender",from);
        startActivity(i);*/

 /*      @Override
       public void onReceive(Context context, Intent intent) {
        // TODO Auto-generated method stub
        Log.e("Broadcast", "__________GCM Broadcast");

                       Bundle extras = intent.getExtras();
                       Intent i = new Intent("broadCastName");
                       // Data you need to pass to activity
                       i.putExtra("message", extras.getString(Config.MESSAGE_KEY));
//when you understand,
           //life becomes a whole lot easier
           //this person wants to send the body of the msg in i.putExtra
                       context.sendBroadcast(i);
        }




}
SR2..baby was used to save the world*/